# Activate LEM through `system2()`.

Activate LEM through [`system2()`](https://rdrr.io/r/base/system2.html).

## Usage

``` r
lem(
  lat = "",
  man,
  con = "",
  dim,
  lab = "",
  mod,
  rec = "",
  des = "",
  sta = "",
  dum = "",
  dat,
  ite = "",
  see,
  path
)
```
