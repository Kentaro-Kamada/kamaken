# Generate random sample from a discrete uniform distribution

This function was in purrr before version 0.3.5 but deprecated in purrr
1.0.0 because it's not related to the core purpose of purrr.

## Usage

``` r
rdunif(n, b, a = 1)
```
