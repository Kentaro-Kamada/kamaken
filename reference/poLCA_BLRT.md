# Goodness of Fit of LCA Models with `poLCA_result`

Goodness of Fit of LCA Models with `poLCA_result`

## Usage

``` r
poLCA_BLRT(
  .poLCA_result_table,
  boot_size = F,
  maxiter = 6000,
  nrep = 3,
  seed = 123,
  workers = future::availableCores()
)
```
